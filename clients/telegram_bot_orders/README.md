# Telegram Bot Orders

Telegram-бот для оформления розничных заказов в Regos.

## Возможности
- Категории номенклатуры (ItemGroup) и просмотр товаров внутри категории.
- Каталог товаров с кнопками-названиями (ReplyKeyboard), карточка товара с фото и описанием.
- Корзина в Redis (TTL) и оформление заказа через DocOrderDelivery/AddFull.
- Карты покупателя с балансом и QR-кодом (штрих-код карты).
- Привязка покупателя к Telegram через `field_telegram_id` (по номеру телефона).
- Получение локации и примечания к заказу.

## Настройки (ConnectedIntegrationSetting)
- `BOT_TOKEN` — токен Telegram-бота.
- `PRICE_TYPE_ID` — вид цены.
- `STOCK_ID` - склад.
- `STOCK_ID_QUANTITY` - склад для расчета количества (если не задан, количество по всем складам).
- `SHOW_ZERO_QUANTITY` — показывать товары с нулевыми остатками.
- `SHOW_WITHOUT_IMAGES` — показывать товары без изображений.
- `ORDER_SOURCE_ID` — источник заказа (`from_id`).
- `MIN_ORDER_AMOUNT` — минимальная сумма заказа (0 — без ограничения).
- `WORK_TIME_ENABLED` — ограничивать время работы (true/false).
- `WORK_TIME_START` — время начала работы (формат `HH:MM`).
- `WORK_TIME_END` — время окончания работы (формат `HH:MM`).
- `CUSTOMER_GROUP_ID` - группа покупателя для создания.
- `WELCOME_MESSAGE` - приветственное сообщение бота (если не задано, используется стандартное).
- `DELIVERY_TYPE_REQUIRED` - запрашивать способ доставки (true/false).
- `ADDRESS_REQUIRED` - запрашивать адрес текстом (true/false).
- `ITEM_GROUP_IDS` - список ID групп товаров через запятую (если пусто, показываются все).
- `ORDERS_DISABLED` - запретить прием заказов (доступны только карты покупателя).
- `ORDER_STATE_TTL` - время хранения состояния заказа в Redis (секунды).

## Режим получения апдейтов Telegram
Глобальная настройка в `config/settings.py`:
- `telegram_update_mode = "webhook"` — использовать вебхук.
- `telegram_update_mode = "longpolling"` — использовать longpolling (вебхук удаляется автоматически).

## UX и навигация
- Главное меню и все действия построены на ReplyKeyboardMarkup.
- Каталог:
  - сначала выбирается категория;
  - список товаров показывается кнопками-названиями;
  - нажание на товар открывает карточку (фото + описание);
  - пагинация по кнопкам «Назад/Вперёд».
- Корзина отображается в формате чека:
  - позиции с ❌, разделены `----`;
  - количество, цена и сумма — на отдельных строках;
  - очистка корзины через кнопку 🗑.

## Флоу заказа
1) Пользователь делится номером телефона — бот ищет покупателя по номеру.
2) Если найден — устанавливается `field_telegram_id`, если нет — создается новый покупатель.
3) Выбор категории → выбор товара → карточка товара → добавление в корзину.
4) В корзине: удаление позиций, очистка, оформление заказа.
5) Перед созданием заказа бот запрашивает локацию и примечание.
6) Документ заказа создается через DocOrderDelivery/AddFull.

## Кеширование и зависимости
- Данные каталога и категорий кешируются в Redis (небольшой TTL).
- Корзина и состояния пользователя хранятся в Redis.
- QR-коды карт формируются через `qrcode` + `pillow` (опционально).
